<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <title>Cookbook</title>
    <%= stylesheet_link_tag 'document' %>
    <%= stylesheet_link_tag 'ingrid' %>
    <%= javascript_include_tag 'prototype' %>
    <%= javascript_include_tag 'rails' %>
    <%= csrf_meta_tag %>
  </head>
  <body>
    <div role="document" id="doc" class="ingrid in-fourths">
      <header>
        <h1>Cookbook</h1>
      </header>
      <div id="sidebar" class="unit">
        <% if logged_in? -%>
          <ul>
            <li><%= link_to "My Recipes", recipes_url %></li>
            <li><%= link_to "All Recipes", all_recipes_url %></li>
            <li><%= link_to "New Recipe", new_recipe_url %></li>
            <li><%= link_to "Logout", logout_url %></li>
          </ul>
        <% end -%>
      </div>
      <div class="unit span-three">
        <%= render :partial => "shared/flash", :locals => {:flash => flash} %>
        <%= yield %>
        <footer>
          <div class="credits">Copyright &copy;<%= Date.today.year %></div>
        </footer>
      </div>
    </div>
  </body>
</html>
